# Soothes the nervous system and promotes sleep.

rounds:
  # ==========================================================
  # PREPARATION
  # ==========================================================

  - section: Preparation
    repeat: 1

    sequence:
      - type: prepare
        behavior: hold
        duration: 3
        label: "PREPARE"

  # ==========================================================
  # BREATHING
  # ==========================================================

  - section: breathing
    repeat: 2

    sequence:
      - type: inhale
        behavior: inhale
        duration: 4
        label: "INHALE"

      - type: hold
        behavior: hold
        duration: 7
        label: "HOLD"

      - type: exhale
        behavior: exhale
        duration: 8
        label: "EXHALE"

  - section: breathing
    repeat: 6
    inherit: true

    sequence:
      - type: inhale
        display: "cycles"

      - type: hold
        display: "cycles"

      - type: exhale
        display: "cycles"

  # ==========================================================
  # RECOVERY
  # ==========================================================

  - section: Recovery
    repeat: 1

    sequence:
      - type: relax
        behavior: hold
        duration: 5
        label: "RELAX"
